// The product's brand identity — defined once here rather than duplicated
// across metadata and page content. Every user-facing surface (page-title
// metadata template, the auth screens, the nav wordmark/mobile header,
// the dev-only design-system reference) reads this constant instead of
// hardcoding the name.
export const siteConfig = {
  name: "MEDIO",
} as const;
